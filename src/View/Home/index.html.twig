{% extends 'layout.html.twig' %}

{% block content %}
  <main class="index">
    <h1>WildForms</h1>
    <h2>Créer et partager des formulaires en ligne</h2>
    <div>
      {% if user %}
        🥳<p>
          You are connected as <strong>{{ user.username }}</strong>!
        </p>
      {% else %}
        <a href="/login">Se connecter</a>
        ou <a href="/register">Créer un compte</a>.
      {% endif %}
    </div>
    <h2>Répondre à des questionnaires</h2>
    {% for form in forms %}
      <h3>{{ form.name }}</h3>
      <a href="saved?id={{ form.id }}">lien vers le formulaire</a>
    {% endfor %}
  </main>
{% endblock %}
